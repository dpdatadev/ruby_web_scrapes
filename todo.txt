#WE BACK BABY
March 5th, 2026 ..
We're coming back to this project.

TODO:
Alert to changes with Diffy (only then scrape and ingest changes)
Extract logic to Rails for frontend configuration and display (later)

sidenote:
./linkserver (scraper_server.go), is a fast microservice that immediately returns
all links on a webpage, passed as a query param, into JSON.
This may not be useful for this project, as getting all the links in Nokogiri
is just one or two line of code. We still have to extract the content.

OLD:
dpdatadev@gmail.com -- 11/27/2022
Refactor into single class that can be called from the command line and pass element configuration.
Call it "LinkScraper" or "LinkFetcher".
Use Postgres module/class to encapsulate the insert logic/data access logic.
enforce DRY

add a few more unit tests
clean up credentials
add rake tasks?
